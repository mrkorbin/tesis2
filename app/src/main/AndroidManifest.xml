<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ve.drkorbin.tesis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".LogActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".MusculoIndivActivity" />
        <activity android:name=".MuscSeleccActivity" />
        <activity android:name=".GuiaActivity" />
        <activity android:name=".IngresoActivity" />
        <activity android:name=".RutinaPrincipianteActivity" />
        <activity android:name=".RutinaAvanzadaActivity" />
        <activity android:name=".ContactanosActivity"></activity>
    </application>

</manifest>